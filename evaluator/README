# StreamLAB Evaluator

Implementation of the StreamLAB monitoring algorithm.

## Command Line Usage

```
streamlab monitor [SPEC] --offline --csv-in [TRACE] --verbosity progress
```


See all available options with `streamlab --help`